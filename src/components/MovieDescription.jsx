import React from "react";
import { useParams } from "react-router-dom";
import ReactPlayer from "react-player/youtube";
import { Link } from "react-router-dom";

// function Description({ movies })
//   const { id } = useParams();
//   const movie = movie.find((el) => el.id === parseInt(id));

// const Description = ({ movies }) => {
//   const { title } = useParams();
//   var movietitles = movies.find((e) => (movn = e.title === title));
//   var moviedetails = movietitles;

//   return (
//     <div>
//       <h1>{movietitles}</h1>
//       <p>{moviedetails.des}</p>
//       <ReactPlayer url={moviedetails.trailer} />
//       <Link to="/"> Home</Link>
//       <Link to="/">Back to Home</Link>

//     </div>
//   );
// };

// export default Description;
